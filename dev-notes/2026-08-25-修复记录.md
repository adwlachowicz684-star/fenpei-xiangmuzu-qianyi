# 2026-08-25 全量审查与修复记录

> 执行：ox-alpha（opencode）。验证：build.ps1 编译通过 + `--roundtrip` 数据自检全部通过。
> 注意：当日 SettingsPanel/BackupService 等文件存在**外部并行编辑**（Trae 侧），本记录仅覆盖本会话改动。

## 一、修复清单（按批次）

### 第一批：P0 阻断级（9/9）
| 编号 | 文件 | 内容 |
|---|---|---|
| A1 | ConfigService / LinkRecordService | JSON 原子写（tmp+File.Move）+ 保存锁；Load 异常兜底 |
| A2 | IconConversion(新) / FolderIconService / PresetIconService | PNG 等源真正转多尺寸 ICO，修假 .ico 图标失效；转换逻辑两服务共享 |
| A3 | AcrylicService:56 | SizeOfData = Marshal.SizeOf<AccentPolicy>()，修毛玻璃颜色失效 |
| A4 | ScreenCaptureService | 删 MoveWindow/Sleep（绝不移动用户窗口）；工具描述同步 |
| A5 | IconService | ConcurrentDictionary 缓存 + Load 结果 Freeze |
| A6 | JunctionService.Create | target TrimEnd+'\\' + mklink 15s 超时 + 输出并行读 |
| A7 | ConfigService.LoadConfig | ReadAllText 入 try，任何异常兜底默认配置 |
| A8 | McpServer.HandleLine | id 用 JsonElement.Clone() 按数值原样回传（W1）；信封手工拼装，无 id 显式 "id":null |
| A9 | App.ResolveDataPaths | 探测序 exe旁数据 → 父目录数据(兼容) → exe目录 |

### 第二批：P1 严重级
- B1 RebuildTabs 末尾补刷新（后升级为异步管线调用）
- B2 Current*Cards 空集合守卫（EmptyCards 回退）；AddFavorite/RemoveFavorite 索引守卫
- B3 SetProcessDpiAwarenessContext 签名 int→IntPtr（高 DPI 截图错位根因）
- B4 AgentSkillService 枚举 IgnoreInaccessible；AgentSkillViewModel.Refresh try/catch
- B5 PresetIconService.MoveIcon 同列下标修正，语义统一「最终落位」；SettingsPanel.CompleteIconDrag 同步对齐
- B6 LinkAgentCatalog 拒绝纯点串链接名；图标名 ValidateNewName 拒绝 "."/".."
- B7 DataSelfCheck 去 `!` 强断言、恒真断言改真实校验
- B8 FolderIconService：读失败返回 null 防误删误覆盖；恢复默认时清文件夹 +s；IconResource 无逗号按 index=0 解析
- B9 MCP stdout 断裂优雅退出；tools/call 业务错误改 isError:true
- B10 CreateLink 部分失败时把已建链接写入账本再抛出
- B12 链接名大小写不敏感查重（AddCustom/ValidateEditName/OnPinChanged）
- B13/D7 构建脚本：dotnet 段 EAP 降级 + Test-DotNetSdk 验证（--list-sdks 退出码）+ vswhere 探测 + 成功清理 TEMP
- B15 标题栏 x:Name="titleBarHost" 直接引用

### 第三批：P2/P3
- C1 新增/跨列移动卡片后补 RefreshCardValidity
- C2 LogViewModel Append/Clear/CopyAll Dispatcher 封送
- C4 全部 ICommand 属性 `??=` 惰性缓存（消除每次 new 的 CommandManager 订阅堆积）
- C6 ApplyEdit 双落盘合并为恰好一次
- C7 ShortcutSettingsViewModel 构造期落盘抑制（_bulk 包裹）
- C8 图标拖拽 UI/服务下标语义统一
- C9 树遍历 ContentElement 保护（MainWindow.FindAncestor / SettingsPanel 三处）
- C11 LinkPickDialog 改绑 CheckBox.Tag 取名
- C12 OnClosed 补 Mcp.Save() 兜底落盘
- C13 Dbg 拖拽日志加 DragDbgEnabled 开关（生产默认关）
- C14 --mcp stdio 重定向预检（IsInputRedirected/IsOutputRedirected）；JsonDocument 泄漏修复（Clone+Dispose）
- C15 SpawnAgent Process Dispose；AutoQuote 引号转义
- C16 LoadPreview Freeze
- C17 OnPopupResizeDelta `?? 240` 死代码清理 + NaN 高度兜底
- D3 Content 构造期初始化（InitBrokerPanels 幂等）

### 第四批：确认后的增强
- **V10 IDialogService**（新 Services/DialogService.cs）：Alert/Confirm/SetClipboard 抽象 + DefaultDialogService（自动封送 UI 线程）；VM 内 MessageBox ×4、Clipboard ×1 已迁移
- **C3/V3/D9 异步刷新管线**：MainViewModel 新增「快照→后台扫描→UI 应用」三段式（BuildScanSnapshot / ScanAllValidity / ApplyScanResult），RefreshAllValidityAsync 带重入保护；同步版 RefreshAllValidity 保留为同管线的串行执行（自检共用单一逻辑源）；F5/ReloadRecordsAndRefresh/ClearInvalid/RebuildTabs 已切异步。RecordSnapshot 不可变快照消除后台扫描与账本变更竞态
- **D5 LocalScaffold 自动避让**：同名 skill 加序号永不覆盖；MCP 返回 renamed 标志与提示文案
- **C10 主题色**：App.xaml 新增 14 个面板专用画刷键（GuideBorderBrush/GuideBgBrush/GuideTextBrush/GuideLineBrush/FolderBarBrush/FolderIconBrush/FolderNameBrush/ChipRow×4/CountTextBrush/ErrorTextBrush/SelectionFillBrush）；TipsPanel 3 处、AgentSkillPanel 11 处（含删除 4 个同名局部定义）、SettingsPanel 6 处已替换

## 二、用户决策记录
| 项 | 决定 |
|---|---|
| B11 skill `_` 拆层约定 | **保留现状不改**（扁平命名靠下划线模拟层级是既定约定） |
| C10 硬编码色 | 替换 + 允许丰富全局资源以减少观感变化 |
| D5 覆盖行为 | 默认自动避让 + MCP 返回提示 |
| A8附带 id:null | 显式输出 |
| C3/V3/D9 异步化 | 启动（已完成） |
| V10 对话框抽象 | 启动（已完成） |
| 文档沉淀 | AI_ERRATA.md（E001-E003）+ 本记录 |

## 三、事故与恢复记录（重要）
- **SettingsPanel/AgentSkillPanel/TipsPanel .xaml 曾被 PowerShell Set-Content 写坏**（GBK 误解码 UTF-8）。恢复链：发现 `E:\_tools\Tools_DIY\分配项目组迁移 - 副本\` 与主项目存在硬链接关系，副本侧保留了损坏前原文 → Copy-Item 复制回主项目 → 用 Edit 工具重放 C10 替换。
- **副本的 SettingsPanel.xaml 是旧迭代版**（缺 settingsHost/iconErrorText/iconDragOverlay 等），不能直接用；期间检测到 Trae 侧并行编辑正在补齐这些元素（09:57 左右），最终由外部编辑完成 settingsHost 改造（settingsScroll 内层滚动被移除，改为不可滚 host + iconListScroll 独立滚动），编译验证通过。
- **教训已沉淀**：AI_ERRATA.md E001/E002/E003。

## 四、待人工验证项（自动化无法覆盖）
1. GUI 实际启动：毛玻璃颜色（A3）、标题栏拖拽/双击最大化（B15）、托盘、设置面板各页签
2. 拖拽体验：分框重排动画、卡片跨列、图标库拖拽下标正确性（B5/C8）
3. MCP 实测：opencode 客户端连 --mcp，验证数字 id 匹配（A8）、isError 错误呈现（B9）、deploy_skill 避让提示（D5）
4. 截图工具：capture_window 不再移动窗口、高 DPI 坐标正确（A4/B3）
5. 异步刷新手感：大收藏量下 F5 是否不再冻结界面；扫描期间快速操作无异常弹窗
6. SettingsPanel 因并行编辑合流，建议重点过一遍图标页签（自适应高度、拖拽反馈层）
